feat(smenu): 引擎侧 GMod 生成命令 + Lua VGUI 面板派发修复

server:
- 新增 game/server/hl2sb_gm_commands.cpp：gm_giveswep / gm_spawn /
  gm_spawnvehicle / gm_spawnnpc / gm_spawnprop。落点用玩家视线射线，
  NPC 装备以 additionalequipment 在 DispatchSpawn 之前写入，
  每次生成都通过 HL2SB_UndoRecord 登记撤销
- 新增 gmod_npcweapon（FCVAR_ARCHIVE）：生成菜单配置的全局 NPC 武器
- 武器走 GiveNamedItem 交给玩家，不再在世界里创建无主武器实体

client:
- public/lua/vgui_controls/lPanel.cpp：Panel___index 的 LPanel 分支缺少向
  "Panel" 元表的兜底（非 LPanel 分支早已有），导致部分面板的 SetSize /
  SetText / OnMousePressed 全为 nil —— 面板画得出来却点不动，重建时抛
  "attempt to call a nil value"。补齐同一段兜底
- 补 IsHovered 绑定（GMod 的名字，指向已有的 IsCursorOver）

删除 C++ 生成菜单 sm_menu_list / sm_menu_legacy，改由 Lua 版承担
